# web

Empty runnable React + Vite web app scaffold for the Tea Shelf MVP.

## Run

```sh
pnpm --filter web dev
```

The root shortcut also runs this app:

```sh
pnpm dev
```

## Check

```sh
pnpm --filter web lint
pnpm --filter web typecheck
pnpm --filter web test
pnpm --filter web build
```
